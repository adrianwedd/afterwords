# Voice wave 1 — ship the 16 listen-QA'd sci-fi voices (2026-08-06)

> **For agentic workers:** Use superpowers:executing-plans to run tasks in order.
> Execute as its own release (v1.0.8-class) — do NOT fold into any other release.

**Status:** SCOPED (written per the 2026-08-06 QA workplan Task 3). Execution not
yet started.
**Policy context:** Per the standing policy in
`2026-08-06-qa-workplan.md` Task 3, voices ship in curated, listen-QA'd waves;
everything un-QA'd stays private by default. This wave covers only the 16
voices cloned 2026-07-29 (rounds 1–3) that passed listen-QA across 3 rounds
plus a 3-agent peer-QA round (codex/agy/hermes: PASS WITH WARNINGS, no
blockers). The 10 round-4 voices (cave-johnson, stanley-narrator, farnsworth,
zapp, naomi, drummer, nog, rom, zek, tom-baker) and all other untracked voices
have no listen-QA on record and are OUT of scope — they stay private.

## Scope — the 16 voices

worf, garak, janeway, sisko, marvin, glados, vader (round 1);
the-book, deep-thought, slartibartfast, c3po (round 2);
quark, riker, majel-computer, bender, amos (round 3).

Each ships 3 files: `voices/{name}.json` + `voices/{name}-qwen3-06b.json` +
`voices/{name}-ref.wav`. Verified on disk 2026-08-06: all 16 have all 3 files
and `family` set on the base profile (check the `-qwen3-06b` variants too at
execution time). Net gallery change: **+16 families / +32 profiles →
118 families / 230 profiles** (from the v1.0.7 baseline of 102/198 — recount
with `git ls-files 'voices/*.json'` at execution time per STRATEGY.md).

## Known QA warnings to carry into release notes / verify at execution

- sisko.json records only 1 of 2 source URLs (notes cite a 25.7s second
  source; `segment_start_s=54`) — fix the JSON or note it.
- quark (21.4s) and majel-computer (25.1s) refs run over the nominal 15s —
  accepted by QA, no action unless a listen-test regresses.
- garak 9.0 chars/s and riker 20.2 chars/s rate outliers — plausible
  (slow talker / fast narration), accepted.
- glados source is vocoder-processed; clone fidelity accepted by owner
  listen-test.
- 6 round-1/2 refs have RMS 0.045–0.067 (below the 0.07 round-3/4 target) but
  within the shipped-gallery distribution — renorm optional, not required.

## Tasks

- [ ] Pre-flight: `git ls-files 'voices/*.json' | wc -l` (expect 198 baseline);
      confirm none of the 16 names collide with tracked voices
      (`git ls-files 'voices/<name>*'` per name — the avasarala overwrite
      near-miss is the lesson here).
- [ ] Validate all 32 JSONs parse, `backend` fields resolve, `family` set on
      BOTH profile variants per voice; WAVs mono 24 kHz 16-bit.
- [ ] Stage named paths only (never `git add -A`): the 48 files
      (16 × ref.wav + 16 × .json + 16 × -qwen3-06b.json).
- [ ] Count sweep (STRATEGY.md recheck-trigger): update README, docs
      og:description / hero counts, CHANGELOG `[Unreleased]` with
      118 families / 230 profiles; note the hero page had pre-existing internal
      drift (hero vs README vs backends-section) — reconcile, don't propagate.
- [ ] Demo clips: decide whether wave-1 voices get demo clips in `docs/`
      (og-metadata guard counts demo clips; keep the guard green either way).
- [ ] `python scripts/internal/check-og-metadata.py` → exit 0.
- [ ] `pytest -q` → green (baseline 670; re-measure).
- [ ] Live check (read-only): server already runs these voices; after commit,
      `curl localhost:7860/health` still 200.
- [ ] Release: roll CHANGELOG → `[1.0.8]`, commit, push, tag `v1.0.8`,
      `gh release create v1.0.8` with the section as notes; CI green on tag.
- [ ] Done-check: `gh release view v1.0.8`; `git ls-files 'voices/*.json' | wc -l`
      = 230; OG guard green; no `voices/muse*`/`voices/vixen*`/round-4 voices
      staged or committed.

## Non-goals

- The 10 round-4 voices and any other untracked/gitignored voices (muse*,
  vixen*, personal) — private by policy.
- The 2026-07-29 repo voice-assignment proposal — delivered-and-parked, not
  pending work.
- Arthur Dent / Ford Prefect / Zaphod — BBC-TV verdict FINAL (unextractable);
  owner declined the radio fallback. Do not re-attempt.
