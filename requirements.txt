# 0.4.1+ broke Qwen3-1.7B ICL (generates EOS on first token); pin below regression.
mlx-audio>=0.4,<0.4.1
soundfile>=0.13,<1.0
fastapi>=0.135,<1.0
python-multipart>=0.0.18,<1.0
uvicorn>=0.42,<1.0

# Cloning deps (noisereduce, faster-whisper) are in requirements-clone.txt.

# OpenVoice v2 is optional because it brings PyTorch/MeloTTS dependencies that
# can conflict with the default MLX-only install. See requirements-openvoice.txt.
