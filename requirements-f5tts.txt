# Optional F5-TTS backend dependencies.
#
# Install these only if you plan to use backend=f5-tts voice profiles:
#   pip install -r requirements-f5tts.txt
#
# F5-TTS code is MIT licensed, but the default SWivid/F5-TTS pretrained
# F5TTS_v1_Base weights are CC-BY-NC 4.0 and block commercial use.
# Keep this isolated from requirements.txt so the default Afterwords install
# remains MLX-only.
torch>=2.12.0
torchaudio>=2.11.0
# f5-tts requires torchcodec but does not pin it; 0.11.x matches torch 2.8.
# FFmpeg symbol mismatch on macOS: if you have Homebrew ffmpeg, set
#   export DYLD_FALLBACK_LIBRARY_PATH="$(python -c 'import torchcodec; import pathlib; print(pathlib.Path(torchcodec.__file__).parent)')"
# before importing to ensure torchcodec's bundled FFmpeg takes precedence.
torchcodec>=0.11.0,<0.12.0
f5-tts>=1.1.20
